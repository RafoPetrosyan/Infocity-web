// Cities
export interface City {
  id: number;
  slug: string;
  name: string;
}

// Emotions
export interface Emotion {
  id: number;
  icon: string;
  color: string;
  name: string;
}

// Categories
export interface Category {
  id: number;
  slug: string;
  name: string;
}
